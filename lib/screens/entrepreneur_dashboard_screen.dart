import 'package:flutter/material.dart';

class EntrepreneurDashboardScreen extends StatelessWidget {
	const EntrepreneurDashboardScreen({super.key});
	@override
	Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Panel del emprendedor')), body: const Center(child: Text('Bienvenido a tu panel PROVEO')));
}
